import React from 'react';
import {StyleSheet, View} from 'react-native';
import {Text} from '@ui-kitten/components';

type PropTypes = {};

function SetInfo(props: PropTypes) {
  return (
    <View>
      <Text>SetInfo</Text>
    </View>
  );
}

const styles = StyleSheet.create({});

export default SetInfo;
